export enum AuditAction {
  CREATED = 'created',
  UPDATED = 'updated',
  DELETED = 'deleted',
  APPROVED = 'approved',
  REJECTED = 'rejected',
  STATUS_CHANGED = 'status_changed',
  SIGNED = 'signed',
  RENEWED = 'renewed',
  PERMISSIONS_UPDATED = 'permissions_updated',
}
